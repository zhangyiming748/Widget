#!/bin/bash
adb shell rm -rf /sdcard/.freedownloadmanager
adb shell touch /sdcard/.freedownloadmanager

adb shell rm -rf /sdcard/.vdevdir
adb shell touch /sdcard/.vdevdir

adb shell rm -rf /sdcard/.zp
adb shell touch /sdcard/.zp

adb shell rm -rf /sdcard/302Gimbal
adb shell touch /sdcard/302Gimbal

adb shell rm -rf /sdcard/AlivcData
adb shell touch /sdcard/AlivcData

adb shell rm -rf /sdcard/Baijiahao
adb shell touch /sdcard/Baijiahao

adb shell rm -rf /sdcard/ByteDownload
adb shell touch /sdcard/ByteDownload

adb shell rm -rf /sdcard/DJI
adb shell touch /sdcard/DJI

adb shell rm -rf /sdcard/GDTDOWNLOAD
adb shell touch /sdcard/GDTDOWNLOAD

adb shell rm -rf /sdcard/geetest
adb shell touch /sdcard/geetest

adb shell rm -rf /sdcard/HttpCache
adb shell touch /sdcard/HttpCache

adb shell rm -rf /sdcard/iapppay
adb shell touch /sdcard/iapppy

adb shell rm -rf /sdcard/Installation
adb shell touch /sdcard/Installation

adb shell rm -rf /sdcard/MidasOversea
adb shell touch /sdcard/MidasOversea

adb shell rm -rf /sdcard/Vod
adb shell touch /sdcard/Vod

adb shell rm -rf /sdcard/bdb53ca9-e86b-4e62-95ac-63005069f317
adb shell touch /sdcard/bdb53ca9-e86b-4e62-95ac-63005069f317

adb shell rm -rf /sdcard/dobest
adb shell touch /sdcard/dobest

adb shell rm -rf /sdcard/libs
adb shell touch /sdcard/libs

adb shell rm -rf /sdcard/mfcache
adb shell touch /sdcard/mfcache

adb shell rm -rf /sdcard/system
adb shell touch /sdcard/system

adb shell rm -rf /sdcard/userexperience
adb shell touch /sdcard/userexperience

adb shell rm -rf /sdcard/.7934039a
adb shell touch /sdcard/.7934039a

adb shell rm -rf /sdcard/Mob
adb shell touch /sdcard/Mob

adb shell rm -rf /sdcard/.BD_SAPI_CACHE
adb shell touch /sdcard/.BD_SAPI_CACHE

adb shell rm -rf /sdcard/.ColombiaMedia
adb shell touch /sdcard/.ColombiaMedia

adb shell rm -rf /sdcard/.DataStorage
adb shell touch /sdcard/.DataStorage

adb shell rm -rf /sdcard/.UTSystemConfig
adb shell touch /sdcard/.UTSystemConfig

adb shell rm -rf /sdcard/.Uc2DataStorage
adb shell touch /sdcard/.Uc2DataStorage

adb shell rm -rf /sdcard/.Uc2UTSystemConfig
adb shell touch /sdcard/.Uc2UTSystemConfig

adb shell rm -rf /sdcard/QQBrowser
adb shell touch /sdcard/QQBrowser

adb shell rm -rf /sdcard/._android.dat
adb shell touch /sdcard/._android.dat

adb shell rm -rf /sdcard/._driver.dat
adb shell touch /sdcard/._driver.dat

adb shell rm -rf /sdcard/.a.dat
adb shell touch /sdcard/.a.dat

adb shell rm -rf /sdcard/Subtitles
adb shell touch /sdcard/Subtitles

adb shell rm -rf /sdcard/.android
adb shell touch /sdcard/.android

adb shell rm -rf /sdcard/Tencent
adb shell touch /sdcard/Tencent

adb shell rm -rf /sdcard/.ants
adb shell touch /sdcard/.ants

adb shell rm -rf /sdcard/.aptoide
adb shell touch /sdcard/.aptoide

adb shell rm -rf /sdcard/.cc
adb shell touch /sdcard/.cc

adb shell rm -rf /sdcard/.com.excean.gspace
adb shell touch /sdcard/.com.excean.gspace

adb shell rm -rf /sdcard/.com.taobao.dp
adb shell touch /sdcard/.com.taobao.dp

adb shell rm -rf /sdcard/.duid
adb shell touch /sdcard/.duid

adb shell rm -rf /sdcard/Xiaomi
adb shell touch /sdcard/Xiaomi

adb shell rm -rf /sdcard/.dygameres.apps
adb shell touch /sdcard/.dygameres.apps

adb shell rm -rf /sdcard/.gs_file
adb shell touch /sdcard/.gs_file

adb shell rm -rf /sdcard/.gs_fs0
adb shell touch /sdcard/.gs_fs0

adb shell rm -rf /sdcard/.gs_fs6
adb shell touch /sdcard/.gs_fs6

adb shell rm -rf /sdcard/.mn_-946569419
adb shell touch /sdcard/.mn_-946569419

adb shell rm -rf /sdcard/.n_a
adb shell touch /sdcard/.n_a

adb shell rm -rf /sdcard/.n_b
adb shell touch /sdcard/.n_b

adb shell rm -rf /sdcard/.n_c
adb shell touch /sdcard/.n_c

adb shell rm -rf /sdcard/.n_d
adb shell touch /sdcard/.n_d

adb shell rm -rf /sdcard/.o_a
adb shell touch /sdcard/.o_a

adb shell rm -rf /sdcard/.omgid
adb shell touch /sdcard/.omgid

adb shell rm -rf /sdcard/.pns
adb shell touch /sdcard/.pns

adb shell rm -rf /sdcard/.profig.os
adb shell touch /sdcard/.profig.os

adb shell rm -rf /sdcard/.tbs
adb shell touch /sdcard/.tbs

adb shell rm -rf /sdcard/.turing.dat
adb shell touch /sdcard/.turing.dat

adb shell rm -rf /sdcard/.turingdebug
adb shell touch /sdcard/.turingdebug

adb shell rm -rf /sdcard/.um
adb shell touch /sdcard/.um

adb shell rm -rf /sdcard/.uxx
adb shell touch /sdcard/.uxx

adb shell rm -rf /sdcard/.vdevdir
adb shell touch /sdcard/.vdevdir

adb shell rm -rf /sdcard/.vivo
adb shell touch /sdcard/.vivo

adb shell rm -rf /sdcard/.zp
adb shell touch /sdcard/.zp

adb shell rm -rf /sdcard/umeng_cache
adb shell touch /sdcard/umeng_cache

adb shell rm -rf /sdcard/qqstory
adb shell touch /sdcard/qqstory

adb shell rm -rf /sdcard/qsvf
adb shell touch /sdcard/qsvf

adb shell rm -rf /sdcard/xy_data
adb shell touch /sdcard/xy_data

adb shell rm -rf /sdcard/keep
adb shell touch /sdcard/keep

adb shell rm -rf /sdcard/Vod
adb shell touch /sdcard/Vod

adb shell rm -rf /sdcard/userexperience
adb shell touch /sdcard/userexperience

adb shell rm -rf /sdcard/supercache
adb shell touch /sdcard/supercache

adb shell rm -rf /sdcard/aplayerlog
adb shell touch /sdcard/aplayerlog

adb shell rm -rf /sdcard/apkDownload
adb shell touch /sdcard/apkDownload

adb shell rm -rf /sdcard/amap
adb shell touch /sdcard/amap

adb shell rm -rf /sdcard/.photoShare
adb shell touch /sdcard/.photoShare

adb shell rm -rf /sdcard/ANRSnap
adb shell touch /sdcard/ANRSnap

adb shell rm -rf /sdcard/BaiduNetdisk
adb shell touch /sdcard/BaiduNetdisk

adb shell rm -rf /sdcard/ByteDownload
adb shell touch /sdcard/ByteDownload

adb shell rm -rf /sdcard/Catfish
adb shell touch /sdcard/Catfish

adb shell rm -rf /sdcard/OSSLog
adb shell touch /sdcard/OSSLog

adb shell rm -rf /sdcard/tbs
adb shell touch /sdcard/tbs

adb shell rm -rf /sdcard/tv.danmaku.bili
adb shell touch /sdcard/tv.danmaku.bili

adb shell rm -rf /sdcard/msc
adb shell touch /sdcard/msc

adb shell rm -rf /sdcard/mipush
adb shell touch /sdcard/mipush

adb shell rm -rf /sdcard/libs
adb shell touch /sdcard/libs

adb shell rm -rf /sdcard/hiresearch_data
adb shell touch /sdcard/hiresearch_data

adb shell rm -rf /sdcard/com.tencent.tim
adb shell touch /sdcard/com.tencent.tim

adb shell rm -rf /sdcard/com.tencent.mobileqq
adb shell touch /sdcard/com.tencent.mobileqq

adb shell rm -rf /sdcard/com.netease.cloudmusic
adb shell touch /sdcard/com.netease.cloudmusic

adb shell rm -rf /sdcard/com.huawei.systemmanager_TMF_TMS
adb shell touch /sdcard/com.huawei.systemmanager_TMF_TMS

adb shell rm -rf /sdcard/com.bilibili.qing
adb shell touch /sdcard/com.bilibili.qing

adb shell rm -rf /sdcard/com.bilibili.comic
adb shell touch /sdcard/com.bilibili.comic

adb shell rm -rf /sdcard/com.bilibili.app.in
adb shell touch /sdcard/com.bilibili.app.in

adb shell rm -rf /sdcard/baidu
adb shell touch /sdcard/baidu

adb shell rm -rf /sdcard/backups
adb shell touch /sdcard/backups

adb shell rm -rf /sdcard/backup
adb shell touch /sdcard/backup

adb shell rm -rf /sdcard/autonavi
adb shell touch /sdcard/autonavi

adb shell rm -rf /sdcard/alipay
adb shell touch /sdcard/alipay

adb shell rm -rf /sdcard/Android/obj/.um
adb shell touch /sdcard/Android/obj/.um

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/.apollo
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/.apollo

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/qzone/zip_cache
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/qzone/zip_cache

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/qzone/head_drop_operaion
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/qzone/head_drop_operaion

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/poke
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/poke

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/newpoke
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/newpoke

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/DoutuRes
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/DoutuRes

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/emoji
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/emoji

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/pddata
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/pddata

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/hotimage
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/hotimage

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/.vipicon
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/.vipicon

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/pttPanelAnimations
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/pttPanelAnimations

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/files/QWallet/.preloaduni
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/files/QWallet/.preloaduni

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/Scribble
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/Scribble

adb shell rm -rf /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/dov_doodle_template
adb shell touch /sdcard/Android/data/com.tencent.mobileqq/Tencent/MobileQQ/dov_doodle_template

adb shell rm -rf /sdcard/.45e36947
adb shell touch /sdcard/.45e36947

adb shell rm -rf /sdcard/.Systemp
adb shell touch /sdcard/.Systemp

adb shell rm -rf /sdcard/.config
adb shell touch /sdcard/.config

adb shell rm -rf /sdcard/AndroLua
adb shell touch /sdcard/AndroLua

adb shell rm -rf /sdcard/LtkPocket
adb shell touch /sdcard/LtkPocket

adb shell rm -rf /sdcard/OSSLog
adb shell touch /sdcard/OSSLog

adb shell rm -rf /sdcard/Subtitles
adb shell touch /sdcard/Subtitles

adb shell rm -rf /sdcard/betasdk
adb shell touch /sdcard/betasdk

adb shell rm -rf /sdcard/panoramas
adb shell touch /sdcard/panoramas

adb shell rm -rf /sdcard/zhihu
adb shell touch /sdcard/zhihu

adb shell rm -rf /sdcard/wormpex
adb shell touch /sdcard/wormpex

adb shell rm -rf /sdcard/com.tencent.mtt
adb shell touch /sdcard/com.tencent.mtt

adb shell rm -rf /sdcard/saottsdk
adb shell touch /sdcard/saottsdk

adb shell rm -rf /sdcard/ithome
adb shell touch /sdcard/ithome

adb shell rm -rf /sdcard/netease
adb shell touch /sdcard/netease

# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/
# adb shell rm -rf /sdcard/
# adb shell touch /sdcard/